# Auth service — business logic lives here, not in routes.
# Currently thin: register and login logic is simple enough to live in crud.
# As rules grow (email verification, rate limiting, audit log), move logic here.
